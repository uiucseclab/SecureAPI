To install node and mongodb:

'brew install node'
'brew install mongodb'

install node app dependencies:
'npm install'

start mongodb database
'mongod --dbpath mongo/'

open new terminal
'node app.js'
